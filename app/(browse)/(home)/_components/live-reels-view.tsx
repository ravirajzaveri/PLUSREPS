"use client";

export const LiveReelsView = () => {
  return (
    <div className="flex items-center justify-center h-full text-muted-foreground">
      Live stream reel view (coming next)
    </div>
  );
};
